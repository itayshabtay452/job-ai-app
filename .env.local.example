# ⚠️ Example envs for local development (do NOT commit real secrets)

# Database — מומלץ להשתמש ב-Neon/Supabase/Railway גם בלוקאל.
# דוגמת Neon (Pooler) — שים לב ל-sslmode=require:
# DATABASE_URL="postgresql://<user>:<password>@<project>-pooler.<region>.aws.neon.tech/<db>?sslmode=require"
DATABASE_URL="postgresql://USER:PASSWORD@HOST:PORT/DB?sslmode=require"

# NextAuth
NEXTAUTH_URL="http://localhost:3000"
# ניתן לייצר סוד כך (Linux/Mac): openssl rand -hex 32
# ב-Windows PowerShell:
#  [BitConverter]::ToString((New-Object Byte[] 32 | %{$_=0}) | %{[byte](Get-Random -Minimum 0 -Maximum 256)}) -replace "-", ""
NEXTAUTH_SECRET="your-secret-here"

# GitHub OAuth (Settings → Developer settings → OAuth Apps)
# Callback URL (local): http://localhost:3000/api/auth/callback/github
GITHUB_ID="xxx"
GITHUB_SECRET="yyy"

# OpenAI
OPENAI_API_KEY="sk-xxxx"

# אופציונלי — מפת מחירון למודלים (כ־JSON string) למדדים:
# OPENAI_PRICE_PER_1K_JSON='{"gpt-4o-mini":{"input":0.15,"output":0.60}}'

# אופציונלי — כיבוי טלמטריה של Next בלוקאל
# NEXT_TELEMETRY_DISABLED=1
